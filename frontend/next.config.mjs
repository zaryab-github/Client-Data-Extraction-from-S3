/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // No hardcoded hosts/URLs here — the app reads NEXT_PUBLIC_* values at runtime.
};

export default nextConfig;
